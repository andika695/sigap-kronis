# Salin berkas ini jadi `.env` lalu sesuaikan dengan kredensial MySQL Anda.
#   cp .env.example .env
#
# `.env` dibaca otomatis oleh docker compose dan TIDAK boleh ikut ter-commit.

SIGAP_DB_USER=root
SIGAP_DB_PASS=
